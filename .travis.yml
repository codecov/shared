language: python
python:
  - "3.7"
install:
    - pip install -r requirements.txt
script:
    - pytest --cov=covreports --cov-report=term-missing
after_success:
    - bash <(curl -s https://codecov.io/bash) -t $CODECOV_TOKEN
