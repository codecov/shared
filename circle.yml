machine:
  python:
    version: 2.7.10
  environment:
    GITHUB_ACCESS_TOKEN: test89dic8e5sqyua46nnpjmixh3ss380wf2dgpw
    GITLAB_ACCESS_TOKEN:
    BITBUCKET_ACCESS_TOKEN:
    BITBUCKET_SERVER_URL:
    BITBUCKET_SERVER_CLIENT_ID:
    CI_PROVIDERS: ci.custom.com

dependencies:
  override:
    - pip install -r tests/requirements.txt -r tests/requirements.txt

test:
  override:
    - py.test tests --cov-report=term-missing
    - codecov
